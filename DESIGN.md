# 寻车助手（C-Position-Trace）产品设计与实现方案

> 一句话：锁车后自动记住车的位置，找车时按"哪一层 → 哪个区 → 最后一箭头指到车"三步把人带到车旁边。
>
> 约束：不依赖车型、不依赖商场任何基础设施；手机为 iPhone（用户设备 iPhone 17）。

---

## 一、产品目标：三大核心能力

找车问题被拆成三个叠加的能力，按价值排序，三个叠加即达成完整目标：

| # | 能力 | 回答的问题 | 失效时的现状 |
|---|------|-----------|-------------|
| 1 | **楼层定位**（最重要） | 车在 B 几层？ | 一层层电梯上下找 |
| 2 | **层内定位**：从楼层入口到车位 | 出电梯后往哪走？（返回时用的入口可能不是离开时的出口） | 在一层几百个车位里瞎转 |
| 3 | **直线定位**：我到车的方位+距离 | 最后几十米，车具体在哪？ | 挨排认车 |

三者是一个漏斗：能力 1 把搜索空间从整栋楼缩到一层；能力 2 缩到一个区域（几十米范围）；能力 3 收口到车。每个能力独立可用、独立降级。

支撑性前提：**锁车瞬间的自动触发**（否则一切记录无从谈起），见 2.4。

---

## 二、三大能力的实现方案

### 2.1 能力一：楼层定位

**核心思路：在"锁车后走出去"时测量，而不是"开车进来"时测量。**

开车进库时 App 很难保证在后台活着（iOS 限制）；但锁车是一个可捕获的触发点（见 2.4）。触发后立即开启气压计（`CMAltimeter`），记录用户**从车位走到地面参考点**（商场大堂/室外）这几分钟内的相对高度变化：

```
楼层 = round( (到达地面时的相对高度) / 层高≈3m ) → "车在你下方 2 层（B2）"
```

- 电梯、扶梯、楼梯全部适用（气压计不依赖步态）；
- 只需要**相对层差**，不需要绝对海拔。几分钟时间窗内天气气压漂移 ≪ 一层的 0.36 hPa，可忽略；
- 找车时反向使用："车在你当前位置下方 2 层"，用户到达目标层后气压归零确认。
- "到达地面"的判定：GPS 恢复 / 活动识别持续步行 + 高度变化停止。

**降级链（依次兜底）：**

1. 气压未记录到 → `CMPedometer.floorsAscended/Descended` 历史查询（7 天内可回查，楼梯/扶梯有效，电梯无效）；
2. → 锁车通知一键拍**柱子上的楼层/分区牌**，Vision OCR 提取"B2"；
3. → 通知栏一键选层（B1/B2/B3，一次点击）。

**硬件要求：零外部硬件**。手机气压计即可（iPhone 6 起全系标配）。

进阶（可选，见 2.5 档位 2）：车内信标自带气压计并广播读数，找车时用"手机气压 − 信标气压"**实时差分**判层——彻底免疫天气漂移，且即使锁车时什么都没记录也能判层。

### 2.2 能力二：层内定位（楼层入口 → 车位）

这是三个能力里唯一存在**纯软件无解场景**的一个，要诚实拆开：

**场景 A：原路返回（回来走的是离开时的同一部电梯/出口）**

- 锁车触发后，PDR 面包屑（`CMPedometer` 步数距离 + `CMDeviceMotion` 航向）静默记录"车位 → 电梯"的轨迹（通常 <300 步，累积误差可接受）；
- 找车时倒序回放："出电梯直行约 40 步 → 右转 → 前方 20 步"。目标不是精确地图，是把人带进**目视/信标可及范围**。

**场景 B：换出口返回（商场很大，从另一部电梯下来）**

面包屑对这部电梯无效。没有室内定位基础设施时，纯软件没有通用解，可行的解按成本排序：

1. **车位号/分区快照（零硬件，永远有效）**：锁车时拍的车位牌（"B2-东区-035"）+ OCR 结构化。每层电梯厅都有分区图，人对照分区标识自行走过去；很多商场的找车机也认这个号。
2. **蓝牙信标接力（一个 AirTag，通用解）**：到达正确楼层后沿主通道行走，手机持续扫描车内信标；地库实测 BLE 可及范围约 20–60 米（车身有屏蔽，信标放中控台/挡风玻璃处为佳），一旦进入范围即无缝接力给能力三。策略提示："沿外圈主车道走一圈"最坏情况覆盖全层。
3. **室内地图叠加（增强，不依赖）**：高德室内图已覆盖部分大型商场，有则把分区/照片钉在平面图上展示。

**结论：能力二 = 面包屑（场景 A）+ 车位快照（保底）+ 信标接力（场景 B 的通用解）。**

### 2.3 能力三：直线定位（我 → 车的方位 + 距离）

室外有 GPS，室内**必须依赖车内放置的外部硬件**。以一个 AirTag 为例，按距离分三层接力：

| 距离 | 技术 | 给用户的形态 |
|------|------|-------------|
| 不限（有人流经过） | Find My 网络：路人 iPhone 匿名上报 | "车大概在这一带"（粗定位，商场地库人流大，效果好） |
| ~20–60m | BLE RSSI | 冷/热指示 + 大致距离（"信号增强，方向对了"） |
| ~10–30m | UWB 精确查找（iPhone 11+；iPhone 17 的 U2 芯片范围约为 U1 的 3 倍） | 箭头 + 米级距离，指到车 |

实现说明：

- AirTag 的 UWB 精确查找只开放给系统"查找"App，我们 App 做到 BLE 接力后**深链跳转查找 App** 完成最后一段；
- 若要"App 内一体化箭头"，需用支持 Apple Nearby Interaction 协议的第三方 UWB 配件（如 Estimote UWB Beacon、Qorvo 模组），成本更高，属档位 2；
- 无任何信标时，能力三降级为：车位照片 + 罗盘雷达（指向"停车快照坐标"，室内仅供参考）。

### 2.4 支撑能力：锁车自动触发

- **主通道**：快捷指令自动化——"当断开车机蓝牙（或 CarPlay）→ 立即运行（无需确认）→ 调用本 App 的'记录停车'App Intent"。iOS 上第三方 App 拿不到蓝牙断开事件，这是唯一可靠正道；首启用 30 秒引导用户建好。触发后 App 获得后台窗口，依次启动：气压记录（能力一）、面包屑（能力二）、拍照提醒。
- **兜底**：CoreMotion 历史回放——`CMMotionActivityManager` 可查 7 天内"驾驶→步行"的切换时刻，即使当时 App 没运行，事后打开也能补录停车时间/步数/楼层数；再兜底是手动一键记录。
- 前提：iPhone 与车机做过普通蓝牙（音频/免提）配对。车机手机 App 类蓝牙钥匙（BLE）不在系统设备列表中，不能作触发器。

### 2.5 外部硬件要求汇总

**商场侧：零要求。全方案不依赖室内地图、信标部署或任何商场配合。**

用户侧按档位：

| 档位 | 硬件 | 成本 | 能力 1 | 能力 2 | 能力 3 |
|------|------|------|--------|--------|--------|
| 0 | 无（仅手机） | ¥0 | ✅ 气压+OCR+选层 | ⚠️ 面包屑(原路) + 车位快照 | ❌ 室内缺失，照片兜底 |
| 1 **（推荐）** | 1 个 AirTag 或 Find My 认证标签，放车内中控台 | ~¥249 | ✅ 同上 | ✅ 信标接力解决换出口 | ✅ Find My 粗定位 + BLE 冷热 + UWB 箭头 |
| 2（极客/量产向） | 自制信标：MCU + BLE + 气压计 + 长续航电池（如 ESP32-C3 + BMP390，物料 ~¥50） | ~¥50 | ✅✅ 差分气压，免疫漂移、无需锁车时记录 | ✅ 可调大发射功率，范围更远 | ✅ App 内一体化（加 UWB 模组则有箭头） |

手机要求：iPhone 6 以上（气压计）；UWB 箭头需 iPhone 11+。**用户的 iPhone 17 全部满足且 UWB 为最强一代。**

---

## 三、开发计划

### Phase 0：可行性验证（1–2 周，全部真机实测）

按风险从高到低：

1. **锁车触发链路**：车机蓝牙断开 → 快捷指令自动化 → App Intent 后台执行，测成功率与延迟，以及触发后后台还能持续记录多久（决定气压/面包屑的记录窗口）。
2. **气压判层（走出式）**：去 2–3 个商场地库，从不同层坐电梯/扶梯/走楼梯到地面，验证层差推算准确率。
3. **BLE/UWB 实测**：AirTag 放车内，实测地库 BLE 发现距离、Find My 网络位置刷新时效、UWB 接力距离。
4. **PDR 面包屑**：150 步内往返回放，误差是否在目视范围。

**产出**：每项一个验证 Demo + 数据结论。不达标项按 2.1–2.3 的降级链降级，不阻塞主线。

### Phase 1：MVP（3–4 周）——能力 1 + 能力 2 保底项

- 锁车自动触发 + 手动记录；气压判层 + 一键选层降级；车位拍照 + OCR；停车历史。
- 找车流程：楼层指示 → 照片/车位号 → （有 AirTag 则）跳转查找 App。

**验收**：商场场景零操作记录成功率 >90%；楼层判定准确率 >90%。

### Phase 2：完整三能力（4 周）

- 面包屑记录与反向回放（能力 2 场景 A）；
- BLE 信标扫描接力 + 冷热指示（能力 2 场景 B + 能力 3）；
- 停车计时/缴费提醒。

**验收**：换出口场景下，从进入正确楼层到找到车 <5 分钟。

### Phase 3：增强（按需）

- 自制差分气压信标（档位 2）、App 内 UWB 箭头（Nearby Interaction 配件）、室内地图叠加、家人共享、Android 版。

---

## 四、工程实现

### 4.1 技术栈

- iOS 原生：Swift + SwiftUI；App Intents（快捷指令入口）；CoreMotion / CoreLocation / CoreBluetooth / Vision（本地 OCR）；SwiftData 本地存储，照片存本地文件。
- **无后端**，数据不出设备（共享功能未来再加轻后端）。
- 传感器/定位/蓝牙全部封装为协议，便于用录制数据做单元测试与模拟回放。

### 4.2 停车-找车状态机

```
[待机]
  --蓝牙断开自动化/手动--> [记录中]
      启动：气压采样（→楼层）、PDR面包屑、拍照提醒通知
  --检测到达地面(GPS恢复/高度稳定)--> [已停车]
      落盘停车快照{入口坐标, 层差, 面包屑, 照片/车位号, 时间}
  --用户点"找车"--> [寻车]
      室外段：步行导航到停车场入口（URL scheme）
      → 楼层段：目标层指示 + 实时气压校核
      → 层内段：面包屑回放 / 车位快照 / BLE扫描接力
      → 收口：UWB跳转查找App / 冷热指示 / 照片
  --BLE近距/再次驾驶--> [待机]（本次归档）
[待机] --7天内补录--> CoreMotion历史回放生成低置信度快照
```

### 4.3 风险与对策

| 风险 | 影响 | 对策 |
|------|------|------|
| 自动化未建/未触发 | 无记录 | CoreMotion 历史补录；手动记录；强引导建自动化 |
| 锁车后后台窗口不足（~30s 起） | 气压/面包屑记录中断 | 申请定位后台模式延长；记多少用多少；车位快照保底 |
| 电梯内气压突变/空调风压 | 层差误差 | 以"高度平台期"分段而非瞬时值；置信度低转一键选层 |
| 车身屏蔽 BLE | 接力距离缩短 | 引导信标放中控台/前挡处；实测校准提示文案 |
| 换出口 + 无信标 | 能力 2 场景 B 缺失 | 车位号/分区快照兜底（产品内明确预期管理） |
| PDR 漂移 | 面包屑带偏 | 限 300 步；给"步数+转向"指令不画精确地图 |

### 4.4 下一步

1. 搭 Xcode 工程骨架（SwiftUI + App Intent"记录停车"）
2. Phase 0 四个验证 Demo：触发链路、气压判层、BLE/UWB 实测、PDR
3. 用户实车 + 商场地库采集数据，回填参数
