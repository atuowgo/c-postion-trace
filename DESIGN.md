# 寻车助手（C-Position-Trace）产品设计与实现方案

> 一句话：锁车即自动记住车的位置（含商场停车场的楼层与室内路径），找车时给出一条能走到车旁边的完整路线。

---

## 一、产品设计

### 1.1 问题定义

用户停车锁车后，回来找车时面临三层递进的困难：

| 层级 | 场景 | 现有方案的缺口 |
|------|------|----------------|
| L1 | 露天/路边停车，忘了停在哪条街 | 车机导航能给两点定位，但需要手动操作，且不自动记录 |
| L2 | 商场多层停车场，不知道在哪一层 | GPS 在室内失效，垂直方向完全无解 |
| L3 | 知道楼层，但停车场很大，不知道往哪走 | 室内无导航，只能靠柱子上的分区标识挨个认 |

**核心洞察**：L2/L3 才是真痛点，而它们的共同根因是——室内没有 GPS，商场也不会为你部署定位基础设施。所以产品的技术路线必须做到**零基础设施依赖**：只用手机自己的传感器解决问题。

### 1.2 目标用户与场景

- 主场景：商场/写字楼多层地下停车场（周末购物 2-4 小时后找车）
- 次场景：路边/园区露天停车（出差、逛街、临时停靠）
- 用户特征：开车通勤/购物的普通人，不愿意为"记车位"做任何额外操作——**自动化是第一设计原则**。

### 1.3 产品原则

1. **零操作优先**：锁车瞬间自动完成记录，用户无感。手动操作只作为兜底和增强。
2. **零基础设施依赖**：不假设商场有室内地图、蓝牙信标或任何配合。有则增强，无则可用。
3. **降级可用**：每个能力都有降级路径，最坏情况也比"什么都没记"强。
4. **隐私本地化**：位置轨迹默认只存本机，云端仅用于多设备/共享场景且需显式开启。

### 1.4 核心功能设计

#### A. 自动记录停车位置（P0）

**触发信号（多信号融合，任一命中即候选，多个命中提高置信度）：**

1. **车机蓝牙/CarPlay/Android Auto 断开** —— 最强信号。用户手机几乎都连车机蓝牙，断开时刻 ≈ 熄火锁车时刻。
2. **活动识别切换**：系统活动识别 API 检测到 `驾驶(IN_VEHICLE) → 步行(WALKING)` 的转换。
3. **iOS Visit / 显著位置变化**：低功耗的兜底信号。

**记录内容（一次"停车快照"）：**

- 最后一个有效 GPS 坐标 + 精度（进入地库前 GPS 会逐渐劣化，取劣化前最后一个高精度点，即**停车场入口坐标**）
- 气压计读数序列（用于楼层推算，见 B）
- 时间戳
- （可选，进入室内后）从车走到电梯/出口的面包屑轨迹（见 C）

#### B. 楼层推算（P1，商场场景的关键）

- 手机气压计精度可分辨约 0.5m 高度变化，一层停车场层高约 3m（≈0.36 hPa）。
- 方案：驾驶中持续低频采样气压。检测到"进入停车场"（GPS 信号骤降 + 仍在驾驶状态）时，以**入口处气压为基准**，停车时刻的气压差 ÷ 层高 = 相对楼层（如 "地下 2 层"）。
- 降级：无气压计或推算置信度低时，锁车后弹一条极轻量的通知："停在几层？B1 / B2 / B3"，一次点击完成（比拍照还快）。

#### C. 面包屑反向导航（P1，"怎么走"的答案）

- 锁车后，App 用 **计步器 + 方向传感器（PDR 步行航位推算）** 静默记录用户从车位走到电梯/出口的轨迹（通常 <200m，累积误差可接受）。
- 找车时**倒序回放**：先把用户导航回停车场入口/电梯（室外用地图步行导航），然后按面包屑给出反向指引："出电梯后直行约 40 步，右转，前方 20 步即到"。
- 不追求米级精度，追求"把人带到目视可见车的范围内"，最后 10 米交给 D（闪灯鸣笛在这个产品里不可用，用雷达/照片兜底）。

#### D. 车位快照：拍照 + OCR（P0，最简单可靠的兜底）

- 锁车后通知栏出现一键快捷入口："拍下车位牌"。
- 对照片跑本地 OCR，自动识别车位号/分区（如 "B2-035"），结构化存储。
- 找车时照片 + 识别出的车位号直接展示——这是**任何传感器方案都失效时的最终兜底**，且商场找车机（很多商场有车位查询终端）也能用这个号。

#### E. 找车模式（P0）

用户点"找我的车"，按场景自动组合：

1. **室外段**：调地图（高德/Apple Maps）步行导航到"停车场入口坐标"。
2. **室内段**：展示楼层（"车在 B2"）→ 面包屑反向指引 → 车位照片/车位号。
3. **雷达模式**：全程可切换的指南针视图——一个指向车的箭头 + 直线距离，简单粗暴但极其好用（室外精确，室内按最后坐标近似）。

#### F. 增值功能（P2+）

- 停车计时与缴费提醒（"已停 2 小时 50 分，商场免费 3 小时"）
- 路边停车的限时/禁停提醒（拍下路边标识牌，OCR 提醒）
- 家人共享车位（"我停好了，位置发给你"）
- 商场室内地图集成（高德室内地图已覆盖部分大型商场，有则显示车位在平面图上的点）
- AR 寻车（相机画面叠加箭头）

### 1.5 竞品与差异化

- 车机 App（如各品牌官方 App）：能给车的 GPS 坐标，但室内无楼层、无路径，且仅限自家新车。
- Apple/Google Maps 的"已停车的车辆"：只记一个点，室内失效，无楼层无路径。
- **本产品差异点：楼层 + 室内反向路径 + 零操作自动记录，且不依赖车型和商场。**

---

## 二、开发计划

### Phase 0：可行性原型（1-2 周）—— 先把风险最大的假设验证掉

技术风险从高到低验证（iOS / iPhone 17 真机）：

1. **快捷指令自动化链路**：建"车机蓝牙断开 → 运行 App Intent"自动化，验证能否无确认、后台静默触发，以及触发延迟；顺带验证 CarPlay 断开触发器。
2. **CoreMotion 历史回放**：停一次车，几小时后查询 `CMMotionActivityManager` / `CMPedometer` 历史，验证能否准确还原停车时刻、步数、上下楼层数。
3. **气压计楼层推算**：实地去 2-3 个商场地库，App 前台开着采集 `CMAltimeter` 曲线，验证层数推算准确率（先不管后台，先验证物理可行性）。
4. **PDR 面包屑**：走 100-200 米回放，看误差是否在"目视可见"范围内；测锁车触发后后台还能记录多久。

**产出**：每项一个验证 Demo + 结论文档。任何一项不成立，对应功能降级为手动方案。

### Phase 1：MVP（3-4 周）

- 自动停车检测（蓝牙断开 + 活动识别双信号）+ 手动一键记录兜底
- 停车快照：坐标 + 时间 + 拍照 + 车位号 OCR
- 找车：地图步行导航 deep link + 雷达指针模式
- 本地存储、停车历史列表

**验收标准**：露天场景零操作记录成功率 > 90%；商场场景"照片 + 入口导航"可用。

### Phase 2：室内能力（4 周）

- 气压楼层推算 + 一键楼层确认降级方案
- 面包屑记录与反向回放
- 停车计时/缴费提醒

**验收标准**：商场场景下用户能被引导到车位 20 米范围内的比例 > 70%。

### Phase 3：增强（按需）

- 室内地图集成、AR 寻车、家人共享、多车管理、iOS/Android 双端补齐

---

## 三、技术实现方案

### 3.1 总体架构

```
┌─────────────────────────────────────────────┐
│                  手机 App                     │
│                                             │
│  停车检测引擎          寻车导航引擎             │
│  ├─ 蓝牙断开监听        ├─ 地图SDK步行导航      │
│  ├─ 活动识别(车→步行)   ├─ 雷达指针(方位+距离)   │
│  ├─ GPS劣化检测        ├─ 面包屑反向回放        │
│  └─ 信号融合状态机      └─ 照片/车位号展示       │
│                                             │
│  传感器采集层           本地存储                │
│  ├─ GPS/定位           ├─ 停车记录(SQLite)     │
│  ├─ 气压计             └─ 照片(本地文件)        │
│  ├─ 计步/方向(PDR)                            │
│  └─ 相机+本地OCR                              │
└─────────────────────────────────────────────┘
        （MVP 纯本地，无后端；共享功能后期加轻后端）
```

### 3.2 关键技术点（iOS 首发，目标机型 iPhone 17 / iOS 26）

iOS 后台限制比 Android 严，但有三张 Android 没有的牌，整个方案围绕它们设计：

1. **快捷指令自动化（Shortcuts Automation）**：iOS 支持"当断开某个蓝牙设备 / 断开 CarPlay 时→立即运行（无需确认）"的个人自动化，且可以在后台调用我们 App 暴露的 App Intent。这是 iOS 上**唯一可靠的"锁车即触发"通道**，绕开了第三方 App 拿不到蓝牙断开事件的限制。首次启动引导用户花 30 秒建好这条自动化。
2. **CoreMotion 历史查询**：`CMMotionActivityManager` 与 `CMPedometer` 都可以查询**过去 7 天**的历史数据（活动类型切换、步数、距离、上下楼层数 `floorsAscended/Descended`）。即使 App 在停车瞬间没有运行，事后也能回放出"几点几分从驾驶切到步行"，作为自动化失灵时的补录兜底。
3. **UWB 精确查找**：iPhone 17 带 UWB 芯片。推荐用户在车里放一个 AirTag——最后 30 米由系统级"精确查找"（方向箭头+米级距离）解决，且 Find My 网络能在地库里借路人 iPhone 上报车的大致位置。这不是竞品，是本产品的最佳搭档：AirTag 管"最后 30 米"，我们管"楼层、路径、从商场里怎么走回停车场"。

| 能力 | iOS 实现 | 备注 |
|------|---------|------|
| 锁车触发 | 快捷指令自动化（车机蓝牙/CarPlay 断开 → 运行 App Intent） | 后台静默执行，主通道 |
| 触发兜底 | `CLLocationManager` Visit 监测 + 显著位置变化唤醒；`CMMotionActivityManager` 历史回放补录 | 自动化没建/失灵时用 |
| 停车定位 | 唤醒时取当前/最近位置；入地库前最后高精度点=入口坐标 | 驾驶中靠显著位置变化周期性唤醒刷新"最后已知位置" |
| 活动识别 | `CMMotionActivityManager`（automotive→walking，支持历史查询） | 低功耗 |
| 气压/楼层 | `CMAltimeter`（iPhone 全系有气压计）；`CMPedometer.floorsAscended/Descended` 历史数据辅助 | 实时气压需 App 在后台活着，做不到时降级为一键选层 |
| PDR 面包屑 | `CMPedometer`（步数/距离）+ `CMDeviceMotion`（航向） | 锁车触发后以后台任务短时运行；只做 <300 步 |
| OCR | Vision 框架（`VNRecognizeTextRequest`，本地） | 车位号正则提取（如 `B2-035`） |
| 步行导航 | Apple Maps `MKDirections`/URL scheme；国内可选高德 URL scheme | MVP 用 URL scheme 最省事 |
| 最后 30 米 | 雷达模式（`CLLocation` 方位+距离）；有 AirTag 则跳转 Find My 精确查找 | |

### 3.3 停车检测状态机（核心逻辑）

```
[待机] --活动识别:开始驾驶--> [驾驶中]
[驾驶中] --GPS精度骤降且仍驾驶--> [疑似进入停车场]（记录入口坐标+基准气压，气压持续采样）
[驾驶中/疑似停车场] --蓝牙断开 或 驾驶→步行--> [已停车]
    → 生成停车快照（坐标、气压序列→楼层、时间）
    → 发通知：可选拍照/确认楼层
    → 静默开启面包屑记录（检测到"回到室外/GPS恢复"后停止）
[已停车] --用户点"找车"--> [寻车模式]
[寻车模式] --接近车位/再次开始驾驶--> [待机]（本次记录归档）
```

### 3.4 技术选型建议

- **首发平台：iOS 原生（Swift + SwiftUI）**，目标设备 iPhone 17（iOS 26）。理由：用户自己的设备就是 iPhone，且本产品 80% 的难点在系统级传感器/后台事件（CoreMotion、CoreLocation、App Intents、快捷指令自动化），原生是唯一顺畅的路径，跨平台框架在这层反而增加成本。Android 版在产品验证后再做。
- 架构：SwiftUI + 一个纯 Swift 的领域层（停车检测状态机、楼层推算、面包屑），传感器/定位封装成协议便于模拟测试。
- 存储：SwiftData（或 Core Data）+ 本地文件存照片；无后端，数据不出设备。
- 地图：MVP 用 Apple Maps / 高德 URL scheme（零集成成本），Phase 2 视需要换 MapKit `MKDirections` 做嵌入式引导。
- 分发：开发期用 Xcode 真机调试 / TestFlight 即可，不急上架。

### 3.5 主要风险与对策（iOS）

| 风险 | 影响 | 对策 |
|------|------|------|
| 用户没建快捷指令自动化 / 自动化被系统延迟 | 错过锁车瞬间 | 首启强引导（30 秒建好）；CoreMotion 历史回放补录：下次打开 App 时自动找回停车时刻并推算位置 |
| 锁车触发时 App 后台运行时间短（~30s） | 面包屑记录被中断 | 用 `beginBackgroundTask` + 定位后台模式延长；记不满就记多少用多少，照片兜底 |
| 地库内无 GPS，唤醒时拿到的坐标是旧的 | 停车点≈入口坐标，有偏差 | 产品上明确表达"停车场入口"而非"车位"；楼层+面包屑+照片补齐入口以内的部分 |
| 气压基准点缺失（驾驶中 App 没活着） | 实时判层不可用 | 用 `CMPedometer` 历史楼层数辅助；置信度低时一键选层 |
| PDR 误差累积 | 面包屑带偏 | 限制 300 步内；按"步数+转向"给指令而非画精确地图；照片兜底 |
| 精确查找依赖 AirTag | 无 AirTag 时最后 30 米靠雷达模式 | AirTag 作为推荐搭配而非必需 |

---

## 四、下一步

1. ~~确认首发平台~~ 已定：iOS（用户设备 iPhone 17）
2. 搭 Xcode 工程骨架（SwiftUI）+ Phase 0 的 4 个验证 Demo（自动化触发、CoreMotion 历史回放、气压采集、PDR）
3. 用户拿真机去一趟商场地库，回填气压/PDR 实测数据，确定楼层推算参数
